sources += files('server.c', 'config.c', 'clippor-selection.c', 'clippor-clipboard.c', 'clippor-database.c', 'clippor-entry.c')
includes += include_directories('include')

subdir('external')
subdir('wayland')
